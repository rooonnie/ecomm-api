spring:
  datasource:
    url: jdbc:h2:mem:ecomm-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

ecomm:
  jwt:
    secret: "ecomm-api-test-jwt-secret-key-min-32b"
    expiration-ms: 3600000
